# Port to run the local Vite development server
PORT=5173

# Base URL for API requests.
# - In development: Use "/api" to proxy requests to the local backend.
# - In production/deployment: Set to your public backend URL (e.g., https://api.example.com).
VITE_API_URL=/api
